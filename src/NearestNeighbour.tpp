#ifndef NEAREST_NEIGHBOUR_TPP_
#define NEAREST_NEIGHBOUR_TPP_

#include <algorithm>

template<class T>
vector<vector<T>> Graph<T>::nearestNeighbour(T startPoint, vector<T> points) {
    vector<vector<T>> routes;

    

    return routes;
}

#endif
